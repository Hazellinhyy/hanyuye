# 中期答辩功能清单

## 已实现功能

- 前台用户门户：公告、可认养猫咪、猫咪详情、线索上报、我的线索、我的申请、我的回访、我的消息、个人中心。
- 后台管理系统：Dashboard、线索核实、猫咪档案、医疗记录、认养审核、协议交接、回访任务、异常预警、公告管理、字典管理、操作日志、全局搜索、CSV 导出。
- 业务闭环：线索上报 -> 志愿者核实 -> 一键建档 -> 医疗维护 -> 发布认养 -> 用户申请 -> 自动评分 -> 初审 -> 终审 -> 协议交接 -> 自动回访 -> 异常预警 -> 管理员处理 -> 消息通知和日志追溯。

## 按角色划分

- 普通用户：浏览猫咪、提交线索、提交认养申请、查看申请进度、提交回访反馈、查看站内消息。
- 志愿者：核实线索、维护猫咪档案、发布认养、初审申请、查看回访和预警、使用后台搜索。
- 医院用户：又称合作医院或医疗协作用户，统一对应 `HOSPITAL` 角色，录入体检、疫苗、绝育、治疗和异常医疗记录，辅助健康状态判断。
- 管理员：终审申请、协议交接、处理预警、公告管理、字典管理、操作日志、Dashboard、CSV 导出。

演示账号默认密码均为 `123456`：普通用户 `user`，志愿者 `volunteer`，医院用户 `hospital`，管理员 `admin`。

## 按业务流程划分

- 线索流程：上报、核实、重复/无效标记、生成猫咪档案。
- 猫咪档案流程：基础信息、照片、健康等级、疫苗、绝育、状态发布。
- 医疗流程：体检、疫苗、绝育、治疗、异常记录、健康状态联动。
- 认养流程：申请表、自动评分、风险等级、志愿者初审、管理员终审。
- 交接流程：协议生成、协议编辑、交接确认、猫咪进入回访期。
- 回访流程：7/30/90 天任务、用户反馈、逾期刷新、异常标记。
- 预警流程：逾期预警、异常回访预警、医疗异常预警、高风险申请预警、管理员处理。
- 系统管理流程：消息通知、公告发布、字典维护、操作日志审计、数据导出、全局搜索。

## 数据库表清单

- 原有核心表：`t_user`、`t_cat`、`t_rescue_report`、`t_application`、`t_medical_record`、`t_notice`。
- MIS 扩展表：`adoption_audit`、`adoption_agreement`、`followup_task`、`followup_record`、`warning_record`、`operation_log`、`system_message`、`dict_item`。

## 关键状态机

- 猫咪状态：`PENDING_VERIFY`、`OBSERVING`、`MEDICAL`、`ADOPTABLE`、`APPLYING`、`ADOPTED`、`FOLLOWING`、`SUSPENDED`、`RETURN_PENDING`。
- 申请状态：`PENDING_INITIAL`、`INITIAL_APPROVED`、`INITIAL_REJECTED`、`PENDING_FINAL`、`FINAL_APPROVED`、`FINAL_REJECTED`、`PENDING_HANDOVER`、`HANDED_OVER`、`CANCELLED`。
- 回访任务状态：`PENDING`、`COMPLETED`、`OVERDUE`、`ABNORMAL`。
- 预警状态：`PENDING`、`PROCESSING`、`HANDLED`、`IGNORED`。

## 系统创新点

- 前台用户门户 + 后台管理系统双界面。
- 多角色权限控制，前端菜单和后端注解共同限制访问。
- 线索到建档闭环，避免猫咪信息停留在普通展示页面。
- 医疗记录与健康状态联动，强化公益认养的责任边界。
- 认养申请自动评分和风险等级，辅助审核决策。
- 双级审核和协议交接，形成可追溯认养链路。
- 协议交接后自动生成 7/30/90 天回访任务。
- 回访异常和逾期自动形成预警。
- 站内消息通知用户关键流程结果。
- 操作日志审计关键管理动作。
- Dashboard 统计线索、猫咪、申请、回访、预警和消息。
- 字典管理为状态和选项配置化预留基础。
- CSV 导出支持中期验收和线下统计。
- 后台全局搜索提升管理效率。
- 猫咪生命周期时间线展示从线索、医疗、认养到回访的完整过程。

## 亮点页面

- `#/`：前台门户和公告。
- `#/cats`、`#/cats/:id`：可认养猫咪和详情。
- `#/admin/dashboard`：管理驾驶舱。
- `#/admin/clues`：线索核实与建档。
- `#/admin/medical`：医疗记录维护。
- `#/admin/adoption/audits`：申请评分与双级审核。
- `#/admin/agreements`：协议交接。
- `#/admin/followups`：回访任务。
- `#/admin/warnings`：异常预警处理。
- `#/admin/logs`：操作审计。

## 后续可扩展方向

- 用户角色管理细化到权限点。
- 字典项驱动全部前端下拉选项。
- 日志分页、导出和审计报表。
- 公告富文本和附件。
- 回访图片上传与审核。
- 移动端或小程序入口。
- AI 风险辅助判断和图像识别增强。
