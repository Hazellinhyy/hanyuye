# 第七阶段演示脚本

## 演示目标

本阶段展示系统从业务流程型 MIS 继续补齐系统管理能力：站内消息、公告管理、字典管理、操作日志和管理驾驶舱。

## 演示准备

1. 启动项目：

```powershell
.\mvnw.cmd spring-boot:run
```

2. 浏览器打开：

```text
http://localhost:8080/
```

3. 如果使用新数据库，依次执行：

```sql
SOURCE src/main/resources/schema.sql;
SOURCE src/main/resources/data.sql;
SOURCE src/main/resources/schema-mis-upgrade.sql;
SOURCE src/main/resources/data-mis-demo.sql;
```

## 演示路径

### 1. 前台门户

- 访问 `#/`。
- 说明：门户首页展示管理员发布的公开公告，并保留猫咪浏览、线索上报入口。
- 点击 `#/cats`，说明原有猫咪浏览能力没有被破坏。

### 2. 普通用户消息中心

- 使用普通用户登录。
- 访问 `#/my/messages`。
- 展示未读/已读筛选。
- 点击单条“标记已读”，再点击“全部标记已读”。
- 说明：线索、申请、协议、回访、预警处理等流程节点会写入站内消息。

### 3. 后台驾驶舱

- 使用管理员登录。
- 访问 `#/admin/dashboard`。
- 展示核心指标：猫咪、线索、申请、回访、预警、未读消息。
- 展示分布指标：猫咪状态、申请状态、回访状态、预警类型。
- 说明：这部分用于中期验收展示 MIS 的统计分析能力。

### 4. 公告管理

- 访问 `#/admin/notices`。
- 新增公告，状态选择 `DRAFT`。
- 点击发布。
- 回到 `#/`，验证已发布公告出现在前台门户。
- 再回后台执行下架或删除。
- 说明：前台只消费发布后的公告，后台维护完整生命周期。

### 5. 字典管理

- 访问 `#/admin/dicts`。
- 输入 `CAT_STATUS` 筛选。
- 新增一个字典项，演示编辑、停用、启用。
- 说明：字典表为后续把硬编码状态逐步配置化预留基础。

### 6. 操作日志

- 访问 `#/admin/logs`。
- 用业务类型筛选，例如 `APPLICATION`、`WARNING`。
- 打开详情，展示操作人、操作类型、业务对象、前后值和备注。
- 说明：关键业务动作已经沉淀审计记录，便于验收和追溯。

### 7. 权限验证

- 切换普通用户，直接访问 `#/admin/notices`。
- 预期显示 403。
- 切换志愿者，访问 `#/admin/dicts`。
- 预期显示 403。
- 切换医院用户，访问 `#/admin/medical`。
- 预期允许访问医疗模块。

## 收尾说明

第七阶段没有迁移前端工程，没有删除旧表和旧接口；新增能力以增量接口、增量 SQL 和静态页面路由扩展为主。下一阶段可以继续做用户角色精细化管理、公告富文本、字典驱动前端下拉项、日志分页导出和更完整的验收测试。

## 第八阶段答辩演示增强

### A. UI 和稳定性说明

- 操作人：讲解人。
- 页面路径：`#/`、`#/admin/dashboard`。
- 操作内容：展示前台门户、后台菜单、状态 Tag、空状态、加载状态、403 页面。
- 预期现象：前台像用户门户，后台像管理系统，权限边界清晰。
- 讲解亮点：系统不是普通展示站，而是按角色、流程和状态驱动的 MIS。

### B. 数据导出

- 操作人：管理员。
- 登录账号：`admin / 123456`。
- 页面路径：`#/admin/cats`、`#/admin/adoption/audits`、`#/admin/followups`、`#/admin/warnings`。
- 操作内容：筛选列表后点击“导出 CSV”。
- 预期现象：下载 CSV 文件，文件字段与当前业务列表一致。
- 讲解亮点：满足管理信息系统常见的数据留存、汇总和线下分析需求。

### C. 后台全局搜索

- 操作人：管理员或志愿者。
- 登录账号：`admin / 123456` 或 `volunteer / 123456`。
- 页面路径：任意后台页面顶部搜索框。
- 操作内容：输入 `CAT260501004`、`APP260626108`、`RP260626001`。
- 预期现象：弹出按猫咪、线索、申请、协议、预警分组的搜索结果。
- 讲解亮点：不引入复杂全文检索，用轻量查询提升后台操作效率。

### D. 权限验证

- 操作人：普通用户、志愿者、医院用户。
- 登录账号：`user / 123456`、`volunteer / 123456`、`hospital / 123456`。
- 页面路径：`#/admin/notices`、`#/admin/dicts`、`#/admin/medical`。
- 操作内容：分别访问管理员和医疗页面。
- 预期现象：普通用户和非授权角色进入 403，医院用户可进入医疗模块。
- 讲解亮点：前端菜单和后端注解权限双重控制。

医院用户又称合作医院或医疗协作用户，登录后默认进入前台门户，点击“进入医院工作台”进入 `#/admin/medical`，用于医疗记录录入和健康信息维护。

### E. 主流程收束

- 操作人：普通用户、志愿者、医院用户、管理员。
- 页面路径：按主流程从 `#/clues/submit` 到 `#/admin/logs`。
- 操作内容：提交线索、核实建档、录医疗、发布认养、提交申请、初审、终审、生成协议、完成交接、提交异常回访、处理预警、查看消息和日志。
- 预期现象：每一步都有状态变化、消息通知、Dashboard 指标和操作日志。
- 讲解亮点：系统覆盖“线索到认养后回访”的完整公益管理闭环。
